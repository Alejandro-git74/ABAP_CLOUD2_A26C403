projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZC_ZDT_INCT_AHJ alias Incident
{
  use create;
  use update;
  use delete;

  use action changeStatus;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;

  use association _History { create; with draft; }
}

define behavior for ZC_ZDT_INCT_H_AHJ alias History
{
  use update;
  use delete;

  use association _Incident { with draft; }
}